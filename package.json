{
  "name": "finwise-proxy",
  "version": "1.0.0",
  "description": "Lightweight CORS proxy for FinWise — proxies FMP and Finnhub APIs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=18"
  }
}
